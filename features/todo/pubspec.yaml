name: todo
description: Todo list page
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  navigation:
    path: ../../navigation
  core:
    path: ../../core
  core_ui:
    path: ../../core_ui
  domain:
    path: ../../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0