name: domain
version: 0.0.1
publish_to: none
description: Project domain module

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core
  core_ui:
    path: ../core_ui
  data:
    path: ../data

  freezed: ^2.4.7
  freezed_annotation: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

  json_serializable: ^6.6.2
  build_runner: ^2.3.3